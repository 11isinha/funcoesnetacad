//3. **Variável Local**
//    - Declare uma função `localExample()` que dentro dela crie uma variável `message` com valor "Variável local" e a imprima.
//    - Fora da função, tente imprimir `message` e observe o erro.
//    - *Tópicos:* variáveis locais, escopo.

function localExample() {
  let message = "Variável local";
  console.log(message);
}

localExample();
