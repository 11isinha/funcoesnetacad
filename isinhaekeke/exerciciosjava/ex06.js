//6. **Data Atual**
//    - Declare `currentDate()` que crie uma variável local `today` com `new Date()` e imprima no console.
//    - *Tópicos:* declaração, variáveis locais.
    
    function currentDate() {
  let today = new Date();
  console.log(today);
}

currentDate();